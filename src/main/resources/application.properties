# SYOS-POS System Configuration

# Database Configuration
db.url=jdbc:mysql://localhost:3306/syos_pos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=9900@tahir
db.driver=com.mysql.cj.jdbc.Driver
db.pool.size=10

# Application Configuration
app.name=SYOS-POS System
app.version=2.0-CCCP2
app.context.path=/syos-pos

# Web Configuration
web.session.timeout=30
web.max.upload.size=10485760

# Logging Configuration
logging.level=INFO
logging.file=logs/syos-pos.log
